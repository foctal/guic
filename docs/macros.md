# Macros

`guic-macros` provides three convenience macros:

- `shared_string!` for literal `gpui::SharedString` values
- `shared_format!` for formatted `SharedString` creation
- `theme_name!` for concise `guic_tokens::ThemeName` construction
