# Popover

`Popover` is a lightweight contextual surface rendered below an inline trigger.

Highlights:

- Controlled open state
- Generic trigger and content support
- Useful for contextual actions and compact helper content

Story coverage:

- `crates/guic-story/src/main.rs`
